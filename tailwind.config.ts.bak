import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./app/**/*.{ts,tsx,mdx}", "./content/**/*.{mdx}"],
  theme: {
    extend: {
      colors: {
        vinario: {
          // Fondo: rosa → morado (degradado)
          "bg-from": "#F8C0E6",
          "bg-to": "#7B2B7B",
          // Uva (cards y botones)
          uva: "#6B1F4A",
          "uva-soft": "#8A3A6B",
          "uva-border": "#9E4C7F",
        },
      },
    },
  },
  plugins: [require("@tailwindcss/typography")],
};

export default config;
